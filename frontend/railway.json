{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npm run build"
  },
  "deploy": {
    "startCommand": "node_modules/.bin/serve -s build -l 3000",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
